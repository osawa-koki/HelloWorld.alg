BEGIN
  # コメントアウト #
  INT a, b, sum;

  print (("Hello, World!", new line));

  # 変数 #
  a := 10;
  b := 20;
  sum := a + b;
  print ((a, " + ", b, " = ", sum, new line));

  # 書式化文字列 #
  printf (($7a$, "Algol68"));
  
  print (("Step ", " yields ", new line))

END